<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/entryRow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp"
    android:paddingHorizontal="10dp"
    android:background="@drawable/pokedex_list_row_bg">

    <ImageView
        android:id="@+id/monIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginEnd="6dp"
        android:contentDescription="@null" />

    <ImageView
        android:id="@+id/caughtBallIcon"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/caught_ball"
        android:contentDescription="@null"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/entryLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/pokedexInk"
        android:textStyle="bold"
        android:textSize="15sp" />

</LinearLayout>
